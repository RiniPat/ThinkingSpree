// The generated/api module holds all the runtime zod validators.
// generated/types is also available but contains duplicates (TS interfaces
// for the same names as the zod constants); only consume it explicitly when
// you need the structural type — e.g. `import type { Founder } from "@workspace/api-zod/types"`.
export * from "./generated/api";
